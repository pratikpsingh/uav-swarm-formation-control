# Scripts

Keep only thin command-line or cluster-launch helpers here. Reusable and testable behavior belongs in
`src/uav_swarm_control`. Scripts must not contain alternative copies of environment, reward, or
algorithm logic.
